#database=H2
spring.datasource.url=jdbc:h2:mem:sampledata;DB_CLOSE_DELAY=-1;INIT=create table if not exists sampledata(user_id varchar(20) primary key, sample_data BLOB);
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.datasource.initialize=true
#spring.datasource.connection-init-sql=insert into sampledata(user_id , sample_data)values ('testUser' ,'datadatadatadata')

logging.file:boot-log.log
logging.level.: info